figma.root.findOne(node => node.type=="COMPONENT_SET" && node.name=="post"); //-> will find a type of item on the page that matches this name - figma.root implies whole document as opposed to figma.page which would be on that page specifically
